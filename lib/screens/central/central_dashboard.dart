import 'package:flutter/material.dart';

class CentralDashboard extends StatelessWidget {
  const CentralDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dashboard Central'),
      ),
    );
  }
}